name: Build and deploy to GitHub Pages

# Runs the full pipeline (fetch -> build -> bundle) and publishes dist/index.html.
# Needs Settings -> Pages -> Source: "GitHub Actions".
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - run: pip install -r requirements.txt

      # Raw inputs are ~100 MB; reuse them until the fetch script changes.
      - name: Cache raw inputs
        id: raw
        uses: actions/cache@v4
        with:
          path: data/raw
          key: raw-${{ hashFiles('pipeline/01_fetch.sh') }}

      - name: Fetch public inputs
        if: steps.raw.outputs.cache-hit != 'true'
        run: bash pipeline/01_fetch.sh

      - name: Build dataset
        run: python pipeline/02_build.py

      - name: Bundle page
        run: |
          python pipeline/03_bundle.py
          touch dist/.nojekyll

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
